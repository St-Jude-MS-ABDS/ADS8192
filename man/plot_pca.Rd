% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_pca}
\alias{plot_pca}
\title{Create a PCA scatter plot}
\usage{
plot_pca(
  pca_result,
  color_by = NULL,
  shape_by = NULL,
  pcs = c(1, 2),
  point_size = 4
)
}
\arguments{
\item{pca_result}{Output from \code{\link{run_pca}()}.}

\item{color_by}{Column name from \code{colData} to map to point color.
Default: \code{NULL} (no color mapping).}

\item{shape_by}{Column name from \code{colData} to map to point shape.
Default: \code{NULL} (no shape mapping).}

\item{pcs}{Integer vector of length 2 specifying which PCs to plot.
Default: \code{c(1, 2)}.}

\item{point_size}{Numeric point size. Default: 4.}
}
\value{
A \code{\link[ggplot2]{ggplot}} object.
}
\description{
Produces a ggplot2 scatter plot of PCA scores, with optional color and
shape aesthetics mapped to sample metadata columns.
}
\examples{
data(airway, package = "airway")
result <- run_pca(airway, n_top = 50)
plot_pca(result, color_by = "dex")
}
